trying to work out jersey (hk2) injection
=========================================

To build
--------
`mvn clean package`

To run
------
`java -jar target/injection-0.1.jar`

To test
-------
`curl -X GET http://localhost:8080/`
